// 题目一：
// 处理“表单重复提交”
// 需求:某表单提交按钮点击后，需调用后端接口完成数据提交。由于网络延迟，用户可能多次点击按钮，导致重复提交，请设计方案解决该问题，
// 要求:
// ① 兼容异步提交场景;
// ② 支持手动取消 “防重复” 状态;
// ③ 不影响按钮原有样式和交互。

// 题目2：
// 需求:前端收集的表单数据常为扁平结构(如{name:张三’'address.province:'北京，'address.city:'北京”})，而后端需要嵌套结构(如{name:"张三', address:{ province:'北京,city:'北京'}})请实现函数 formatFormData(rawData)，将扁平结构的表单数据转为嵌套结构，要求:① 支持多级嵌套(如 a.b.c转为{a:{b;{c 值}}});②空值处理(如“、undefined、null，可配置是否保留);③ 支持数组格式(如 hobby[0]='篮球' 转为 { hobby:「'篮球”]})

// 题目3：
// 实现“深度对比两个对象是否相等“需求:开发一个工具函数isDeepEqual(a,b)，判断两个对象(或数组、基本类型)是否深度相等。要求:① 支持基本类型(string/number/boolean/nul/undefined/Syinbol)对比;② 支持数组对比(元素顺序,深度均需一致);③ 支持对象对比(键名、键值深度均需一致，忽略原型链上的属性);④ 避免循环引用导致的死循环(如 a={b: a})。
